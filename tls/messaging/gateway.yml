parameters:
  
  salt:
    minion:
      trusted_ca_minions:
        - cfg01.${_param:cluster_domain}
  neutron:
    gateway:
      message_queue:
        port: 5671
        ssl:
          enabled: True
