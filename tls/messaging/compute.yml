parameters:

  salt:
    minion:
      trusted_ca_minions:
        - cfg01.${_param:cluster_domain}
            
  neutron:
    compute:
      message_queue:
        port: 5671
        ssl:
          enabled: True
  nova:
    compute:
      message_queue:
        port: 5671
        ssl:
          enabled: True
